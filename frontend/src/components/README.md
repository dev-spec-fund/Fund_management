# Shared UI components

New reusable UI should be added here instead of expanding `App.jsx`.
The next safe extraction targets are Modal, Field/MoneyField, MemberCard and ActivityRow.
Page-level extraction should be done incrementally with build verification.
